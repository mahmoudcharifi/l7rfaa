import 'package:flutter/material.dart';

class EditArtisanProfileScreen extends StatelessWidget {
  const EditArtisanProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Modifier le profil"),
      ),
      body: const Center(
        child: Text("À venir..."),
      ),
    );
  }
}